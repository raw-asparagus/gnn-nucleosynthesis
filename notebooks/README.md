# notebooks/ — exploratory only

Nothing here is load-bearing. Any analysis that produces a number cited in
docs/tex must be promoted to a script under `scripts/` (and its outputs logged
in `RESULTS.md`) before it counts.

`phase0/` — the Phase-0 pipeline notebook series (one notebook per flowchart
node, visualizing RESULTS.md-backed measurements); see `phase0/README.md`.
