% =====================================================================
%  Tier 1 -- Microphysics.  Standalone report.
%
%  Source:  study/tier1.md  (audited 2026-08-13; audit log study/tier1-audit.md)
%  Build:   latexmk -lualatex -cd study/tier1-report/main.tex
%
%  This file re-architects the Markdown source from its node-by-node order
%  (Part I rate theory -> II REACLIB -> III detailed balance -> IV screening
%  -> V weak -> VI reconciliation -> VII guards -> VIII open register) into
%  EPISTEMIC LAYERS: theory and derivation first, then the compiled
%  evaluator, then verification, then executable policy, then the open
%  register.  No content is omitted; Appendix B is the concordance that
%  proves it, and check_coverage.py is the mechanical audit.
% =====================================================================
\documentclass[11pt,a4paper]{article}

% ---------- fonts and encoding (LuaLaTeX) ----------
\usepackage{fontspec}
\usepackage{unicode-math}
\setmainfont{TeX Gyre Pagella}
\setmathfont{TeX Gyre Pagella Math}
\setmonofont{DejaVu Sans Mono}[Scale=MatchLowercase]
% DejaVu Sans Mono carries the box-drawing and geometric-shape blocks, so any
% Unicode surviving into verbatim still renders instead of vanishing.

% ---------- layout ----------
\usepackage[a4paper,margin=2.4cm,bottom=2.8cm]{geometry}
\usepackage{microtype}
\usepackage{parskip}
\linespread{1.04}
\emergencystretch=3em
\setcounter{tocdepth}{3}
\setcounter{secnumdepth}{3}

% ---------- maths ----------
\usepackage{amsmath}
\allowdisplaybreaks

% ---------- tables ----------
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{longtable}
\renewcommand{\arraystretch}{1.18}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{R}[1]{>{\hsize=#1\hsize\raggedright\arraybackslash}X}

% ---------- lists ----------
\usepackage{enumitem}
\setlist{itemsep=2pt,topsep=4pt,parsep=0pt}

% ---------- graphics ----------
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,shapes.misc,fit,
                backgrounds,calc,decorations.pathmorphing}
\usepackage{float}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf,skip=6pt}

% ---------- callouts ----------
\usepackage[most]{tcolorbox}
\tcbuselibrary{breakable,skins}

% ---------- citations (inline author-year, backed by \cite) ----------
\usepackage[authoryear,round]{natbib}
\bibpunct{(}{)}{;}{a}{}{,}
\setlength{\bibsep}{2pt plus 0.4ex}

% ---------- hyperref must load last ----------
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={Tier 1 -- Microphysics: reaction-rate theory, detailed balance,
            plasma corrections, the weak sector, and the MESA/pynucastro
            reconciliation for a conservation-by-construction GNN emulator
            of silicon burning},
  pdfsubject={Personal study material; not project spec},
  pdfkeywords={thermonuclear reaction rates, REACLIB, detailed balance,
               partition functions, plasma screening, electron capture,
               silicon burning, MESA, pynucastro}}

% =====================================================================
%  Notation macros.  Repo convention: superscript isotopes in prose,
%  lowercase handles (co55) in anything code-adjacent.
% =====================================================================
\newcommand{\nuc}[2]{\ensuremath{{}^{#2}\mathrm{#1}}}
\newcommand{\Ye}{\ensuremath{Y_{e}}}
\newcommand{\dYe}{\ensuremath{\Delta Y_{e}}}
\newcommand{\Tn}{\ensuremath{T_{9}}}
% \code inserts zero-width breakpoints after / _ . : - so that long
% identifiers (tests/test_flux_compile.py::TestScreeningVectorized) wrap
% instead of running into the margin.
\ExplSyntaxOn
\tl_new:N \l__code_tl
\NewDocumentCommand{\code}{m}{%
  \tl_set:Nn \l__code_tl {#1}
  \regex_replace_all:nnN { \c{_} }  { \c{_}\c{allowbreak} }  \l__code_tl
  \regex_replace_all:nnN { / }      { /\c{allowbreak} }      \l__code_tl
  \regex_replace_all:nnN { \. }     { .\c{allowbreak} }      \l__code_tl
  \regex_replace_all:nnN { : }      { :\c{allowbreak} }      \l__code_tl
  \texttt{\tl_use:N \l__code_tl}%
}
\ExplSyntaxOff
\newcommand{\msn}[1]{\texttt{mesa\_#1}}
\newcommand{\Msun}{\ensuremath{M_{\odot}}}
\newcommand{\gcc}{\ensuremath{\mathrm{g\,cm^{-3}}}}
\newcommand{\enuc}{\ensuremath{e_{\mathrm{nuc}}}}
\newcommand{\dd}{\mathrm{d}}
\newcommand{\Tr}{^{\mathsf{T}}}
\newcommand{\arxiv}[1]{\href{https://arxiv.org/abs/#1}{arXiv:#1}}
\DeclareMathOperator{\asinh}{asinh}
\DeclareMathOperator{\cond}{cond}
\newcommand{\degree}{\ensuremath{^{\circ}}}
% ticks and warning glyphs used inline in the source
\newcommand{\yes}{\ensuremath{\checkmark}}
\newcommand{\no}{\ensuremath{\times}}

% ---------- Tier 1 notation ----------
\newcommand{\sigv}{\ensuremath{\langle\sigma v\rangle}}
\newcommand{\Nasigv}{\ensuremath{N_{A}\langle\sigma v\rangle}}
\newcommand{\dNs}{\ensuremath{\Delta N_{s}}}
\newcommand{\rhoye}{\ensuremath{\rho Y_{e}}}
\newcommand{\fac}{\ensuremath{\mathrm{fac}}}
\newcommand{\muE}{\ensuremath{\mu_{e}}}
\newcommand{\gtil}{\ensuremath{\tilde\gamma}}
\newcommand{\ztil}{\ensuremath{\tilde z}}
\newcommand{\Rset}{\ensuremath{\mathcal{R}}}
\newcommand{\Pset}{\ensuremath{\mathcal{P}}}
% reduced (barred) de Broglie wavelength; not guaranteed by unicode-math
\newcommand{\lbar}{\ensuremath{\mathchar'26\mkern-9mu\lambda}}

% =====================================================================
%  Provenance tags.
%
%  The source carries two provenance markers that tier0.md did not use and
%  the tier-0 report therefore has no precedent for:
%
%    [derived here]  x34 -- reproducible from the snippets given in the
%                           source, but NOT a RESULTS.md row.
%    [RESULTS <date>] x34 -- a measured project number whose source of truth
%                           is RESULTS.md, with script + commit + data
%                           provenance.  If a number here disagrees with
%                           RESULTS.md, RESULTS.md wins.
%
%  Both must survive the conversion; check_coverage.py counts them.
% =====================================================================
\definecolor{provrule}{HTML}{6B6151}
\newcommand{\derivedhere}{\,{\footnotesize\textcolor{provrule}{\textsc{[derived here]}}}}
\newcommand{\resultsref}[1]{\,{\footnotesize\textcolor{provrule}{[\textsc{results}~#1]}}}

% =====================================================================
%  Callout environments.
%  Five kinds, plus a repo-pointer box; all breakable, all accepting \par.
% =====================================================================
\definecolor{resultrule}{HTML}{1F5C8B}
\definecolor{warnrule}  {HTML}{A33A16}
\definecolor{rulerule}  {HTML}{4A3B77}
\definecolor{exrule}    {HTML}{1E6B4F}
\definecolor{asiderule} {HTML}{5A5A5A}
\definecolor{pointrule} {HTML}{6B6151}

\newcommand{\warnglyph}{\textbf{\textcolor{warnrule}{!\!}}}

\tcbset{
  calloutbase/.style={
    breakable, enhanced, sharp corners,
    boxrule=0pt, leftrule=2.2pt, colback=white,
    left=8pt, right=6pt, top=5pt, bottom=5pt,
    parbox=false, before skip=8pt, after skip=8pt}}

\newtcolorbox{result}  {calloutbase, colframe=resultrule,
                        colback=resultrule!4!white}
\newtcolorbox{warn}    {calloutbase, colframe=warnrule,
                        colback=warnrule!5!white}
\newtcolorbox{invariant}{calloutbase, colframe=rulerule,
                        colback=rulerule!5!white}
\newtcolorbox{exercise} {calloutbase, colframe=exrule,
                        colback=exrule!4!white}
\newtcolorbox{aside}    {calloutbase, colframe=asiderule,
                        colback=asiderule!4!white}

% Repo pointers: "In the code:", "Files:", "Oracle:", "Notebook:".
\newtcolorbox{repopointer}[1][]{
  breakable, enhanced, sharp corners, boxrule=0pt, leftrule=1.4pt,
  colframe=pointrule, colback=pointrule!5!white,
  left=7pt, right=6pt, top=4pt, bottom=4pt, fontupper=\small,
  parbox=false, before skip=7pt, after skip=7pt, #1}

% =====================================================================
%  Self-check environment.  44 questions in total across the document,
%  every one preserved from the source; the layered architecture
%  redistributes them, and Appendix B records every relocation.
% =====================================================================
\newlist{selfcheckenum}{enumerate}{1}
\setlist[selfcheckenum]{label=\arabic*., leftmargin=1.9em,
                        itemsep=4pt, topsep=4pt}
\newenvironment{selfcheck}
  {\par\addvspace{6pt}\noindent
   {\bfseries\sffamily\small SELF-CHECK}\par\addvspace{2pt}
   \begin{selfcheckenum}}
  {\end{selfcheckenum}\par\addvspace{6pt}}

% =====================================================================
%  Verbatim listings that must keep their exact spacing (coefficient
%  dumps, table headers, Fortran branches, code paths).
% =====================================================================
\usepackage{fancyvrb}
\DefineVerbatimEnvironment{srclisting}{Verbatim}
  {fontsize=\small, xleftmargin=1.2em, samepage=false}
\DefineVerbatimEnvironment{srclistingtiny}{Verbatim}
  {fontsize=\footnotesize, xleftmargin=0.6em, samepage=false}

% =====================================================================
\begin{document}

\include{parts/00-front}

% ---- Layer A: theory and derivation --------------------------------
\include{parts/01-rate-theory}
\include{parts/02-reaclib}
\include{parts/03-detailed-balance}
\include{parts/04-plasma}
\include{parts/05-weak}

% ---- Layer B: the compiled evaluator --------------------------------
\include{parts/06-evaluator}

% ---- Layer C: verification ------------------------------------------
\include{parts/07-verification}

% ---- Layer D: executable policy -------------------------------------
\include{parts/08-guards}

% ---- Layer E: the open register -------------------------------------
\include{parts/09-open}

% ---- Layer F: handoff -----------------------------------------------
\include{parts/10-handoff}

\appendix
\include{parts/A-sectionmap}
\include{parts/B-concordance}

\include{parts/bibliography}

\end{document}
