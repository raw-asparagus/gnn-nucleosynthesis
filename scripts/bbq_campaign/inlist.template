! bbq inlist — Step-6 rerun campaign (generated by make_campaign.py; do not edit)
! Config pinned to the shipped-label configuration (ADR 0005): stock defaults
! for solver + weaklib ordering; screening chugunov; MESA r23.05.1.

&bbq
   net_name = '{net}.net'
   ! solver tolerances = bbq defaults = label config
   max_steps = 1000000
   eps = 1d-8
   odescal = 1d-10
   stptry = 0
   use_hydrostatic = .true.
/

&sampling /

&random /

&profile /

&hydrostatic
   logT = {logT}
   logRho = {logRho}
   times_from_file = .true.
   input_filename = 'times.txt'
   input_composition_filename = 'comp.txt'
   output_filename = 'output.txt'
/

&eos /

&rates /

&nuclear
   ! defaults, pinned explicitly: label screening + weaklib ordering
   screening_mode = 'chugunov'
   use_suzuki_weak_rates = .false.
/
